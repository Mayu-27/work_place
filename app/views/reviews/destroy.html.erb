<%= render "shared/header" %>

<div class="contents-row">
  <div class="success">
    <h3>削除が完了しました。</h3>
    <%= link_to "マイページで確認する", user_path(current_user), class: "click-btn" %>
  </div>
</div>

<%= render "shared/footer" %>