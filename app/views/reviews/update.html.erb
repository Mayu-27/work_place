<%= render "shared/header" %>

<div class="contents row">
  <div class="success">
    <h3>更新が完了しました。</h3>
    <%= link_to "マイページで確認する", spot_path(@spot), class: "click-btn" %>
  </div>
</div>

<%= render "shared/footer" %>